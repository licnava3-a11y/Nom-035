/**
 * PWAUpdateBanner — deshabilitado
 * El Service Worker fue eliminado para evitar loops de recarga en iOS Safari.
 * Este componente es un stub vacío para mantener compatibilidad con App.tsx.
 */
export function PWAUpdateBanner() {
  return null;
}
